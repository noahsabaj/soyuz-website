import type { PageLoad } from './$types';

export const load: PageLoad = () => ({
	title: 'Examples - Soyuz',
	description:
		'Browse example Soyuz scripts, from gears to organic shapes, and learn procedural 3D modeling with Signed Distance Fields.'
});
